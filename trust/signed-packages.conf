# AUR packages whose upstream publishes PGP-signed artifacts. For
# these, bin/shelly-update REQUIRES the PKGBUILD to declare the pinned
# fingerprint in validpgpkeys — a PKGBUILD update that removes or swaps
# the signature check aborts the run instead of silently downgrading to
# checksum-only trust.
# Format: package fingerprint
1password 3FEF9748469ADBE15DA7CA80AC2D62742012EA22
1password-cli 3FEF9748469ADBE15DA7CA80AC2D62742012EA22
