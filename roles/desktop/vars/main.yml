---
desktop_pacman:
  - 7zip
  - unrar
  - sof-firmware
  - cachyos-gaming-applications
  - ffmpeg
  - signal-desktop
  - moonlight-qt
  - spotify-launcher
  - zed
  - ghostty
  - wl-clipboard
  - plasma-keyboard
  - qrca

# AUR manifest — not installed by this role. bin/kaji reads every
# `<role>_aur` list, reviews each PKGBUILD (Claude + human), verifies
# sources, and installs via Shelly at the approved commit. Keep
# entries as literal `- name` lines: the list is parsed without a YAML
# library.
desktop_aur:
  - google-chrome
  - wifiman-desktop
  - zoom
  - darkly

desktop_user_groups:
  - video

desktop_pacman_remove:
  - alacritty
  - firefox
  - konsole
